using System.Collections.Generic;
using System.Threading.Tasks;

namespace PaySharp.API.Services.Contracts
{
    public interface ITransactionService
    {
        //Task<TransactionDTO> SendTransactionAsync(string senderAccountNumber,
        //    string receiverAccountNumber, string description, decimal amount);
        //Task<TransactionDTO> SaveTransactionAsync(string senderAccountNumber,
        //    string receiverAccountNumber, string description, decimal amount);
        //Task<TransactionDTO> EditTransactionAsync(string senderAccountNumber,
        //    string receiverAccountNumber, string description, decimal amount, long transactionId);
        //Task<List<TransactionDTO>> GetUserTransactionsByAccount(long accountId,
        //    long userId, int currentPage, int pageSize = 5);
        //Task<List<TransactionDTO>> GetAllUserTransactions(long userId, int currentPage, int pageSize = 5);
        //Task<long> GetTransactionsCount(long userId, long? accountId);
        //Task<TransactionDTO> SendSavedTransactionAsync(long transactionId);
    }
}
